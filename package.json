{
  "name": "steam-api-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "A proxy for Steam Web API that hides the API key",
  "scripts": {
    "test": "echo \"No tests specified\" && exit 0",
    "format": "prettier --write .",
    "lint": "eslint ."
  },
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^8.0.0",
    "prettier": "^3.0.0"
  }
}